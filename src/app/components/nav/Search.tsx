import Button from '../Button'

const Search = () => {
  return (
    <div className="relative">
      <Button color="gray-text">
        搜尋
      </Button>
      {/* TODO: SearchModal  */}
    </div>
  );
};

export default Search;